/**
 * <h1>So What *is* the Best-Case for Finding a Method via Reflection?</h1>
 * <h2>Sourced from the Java&trade; Specialist's Newsletter</h2>
 * <h3>Issue 254</h3>
 * Abstract:
 * We now look at why the best-case scenario for a getMethod() call is O(n),
 * not O(1) as we would expect. We also discover that the throughput of
 * getMethod() has doubled in Java 9.
 */
package com.androsaces.javaessentials.issue254;